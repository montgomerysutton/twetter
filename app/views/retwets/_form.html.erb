<%= form_for(@retwet) do |f| %>
  <% if @retwet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@retwet.errors.count, "error") %> prohibited this retwet from being saved:</h2>

      <ul>
      <% @retwet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :twet_id %><br>
    <%= f.number_field :twet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
